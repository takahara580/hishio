<% if current_customer %>
  <div>
    <%= form_with model: [@item, @item_comment], url: item_item_comments_path(@item), local: false do |f| %>
      <%= f.text_field :comment, rows: '5', class: 'form-control', placeholder: "コメントをここに" %>
      <%= f.submit "送信する" %>
    <% end %>
  </div>
<% else %>
  <p>コメントを投稿するにはログインが必要です。</p>
<% end %>